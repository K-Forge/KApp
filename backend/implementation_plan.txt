# Backend Implementation Plan - Dynamic Dashboard (Alpha)

## Goal
Create a dynamic dashboard for Students, Professors, and Admins with JWT authentication and DTO-based communication.

## Architecture
- **Framework**: Spring Boot 3.5.7
- **Language**: Java 21
- **Database**: PostgreSQL
- **Security**: Spring Security + JWT (Stateless)
- **Data Transfer**: DTOs (Data Transfer Objects)

## Roles & Features
1.  **Student**:
    -   View enrolled courses.
    -   View pending and submitted assignments.
    -   Submit assignments (upload link/text).
2.  **Professor**:
    -   View teaching courses.
    -   View students in specific courses.
    -   CRUD Assignments (Create, Read, Update, Delete).
    -   Grade submissions.
3.  **Admin**:
    -   Global CRUD for Courses, Professors, Students, and Assignments.

## Implementation Steps

### 1. Dependencies & Database
-   [x] **Update pom.xml**: Add `jjwt-api`, `jjwt-impl`, `jjwt-jackson`.
-   [x] **Update init.sql**: Add `assignment` and `submission` tables.

### 2. Security Layer (JWT)
-   [x] **JwtTokenProvider**: Generate and validate tokens.
-   [x] **JwtAuthenticationFilter**: Intercept requests, parse token, set SecurityContext.
-   [x] **SecurityConfig**: Configure filter chain, disable CSRF (stateless), set permissions.
-   [x] **CustomUserDetailsService**: Load user details from DB (Person/Member).

### 3. Domain & DTOs
-   [x] **Entities**: `Assignment`, `Submission`. (Ensure `User`/`Role` mapping exists).
-   [x] **DTOs**:
    -   `LoginRequest`, `JwtResponse`
    -   `AssignmentDTO`, `SubmissionDTO`
    -   `CourseDTO`, `StudentDTO`

### 4. Repositories
-   [x] `AssignmentRepository`
-   [x] `SubmissionRepository`
-   [x] `CourseGroupRepository` (verify existing)
-   [x] `StudentCourseRepository` (verify existing)

### 5. Services
-   [x] **AuthService**: Login logic.
-   [x] **StudentService**: `getEnrolledCourses`, `getAssignments`, `submitAssignment`.
-   [x] **ProfessorService**: `getTeachingCourses`, `createAssignment`, `gradeSubmission`.
-   [x] **AdminService**: Generic CRUD methods (Read implemented).

### 6. Controllers
-   [x] **AuthController**: `/auth/login`
-   [x] **DashboardController** (Implemented as Role Controllers):
    -   `GET /api/student/courses`
    -   `GET /api/professor/courses`
-   [x] **AssignmentController** (Implemented in Role Controllers):
    -   `POST /api/professor/assignments`
    -   `GET /api/student/assignments/pending`
    -   `POST /api/student/assignments/{id}/submit`

## Notes
-   Skip unit tests for Alpha.
-   Use `Lombok` for boilerplate.
-   Map `employee_role_enum` to `ROLE_PROFESSOR`, `ROLE_ADMIN`.
